from chatbot import export_resume_to_pdf, export_resume_to_docx

def export_resume_to_pdf_file(html_path, output_path):
    return export_resume_to_pdf(html_path, output_path)

def export_resume_to_docx_file(html_path, output_path):
    return export_resume_to_docx(html_path, output_path)
